import { SunMoon } from 'lucide-react'

import { Button } from 'shared/ui/Button'

export const ThemeSelectorLoading = () => {
    return <Button icon={SunMoon} size={'iconSm'} variant={'ghost'} disabled />
}
